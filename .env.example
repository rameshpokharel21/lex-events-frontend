# Copy to .env (local) — Vite reads it at build/dev time.
# Local backend:
VITE_API_URL=http://localhost:9000/api
# Production (set in Vercel -> Settings -> Environment Variables, then redeploy):
# VITE_API_URL=https://events-api.lexnepali.com/api
